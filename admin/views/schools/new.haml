%ul.nav.nav-tabs
  %li=link_to tag_icon(:list, pat(:list)), url(:schools, :index)
  %li.active=link_to tag_icon(:plus, pat(:new)), url(:schools, :new)

.tabs-content
  -form_for :school, url(:schools, :create), :class => 'form-horizontal' do |f|
    =partial 'schools/form', :locals => { :f => f }
